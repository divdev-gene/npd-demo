## NTD (New Technology Development) Flow

`typeOfWork === "NTD"`. Starts at **stage 2**.

**Wizard (step 2):** Similar left column layout to NCD plus type-specific right column fields. NTD starts at stage 2.

**Cost Innovation** also starts at stage 2 and follows a similar wizard layout.

No dedicated per-stage documentation exists yet — stage behavior follows the general NPD pattern (`updateNPD` + `setActiveStage` on submit).
