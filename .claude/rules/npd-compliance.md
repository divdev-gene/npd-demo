## Compliance / Regulatory Flow

`typeOfWork === "Compliance / Regulatory"`. 6 stages. Starts at **stage 3**.

**Wizard (step 2):** Similar left column layout to NCD plus type-specific right column fields. Compliance starts at stage 3.

No dedicated per-stage documentation exists yet — stage behavior follows the general NPD pattern (`updateNPD` + `setActiveStage` on submit).
