## NTD Flow — Detailed Stage Reference

`typeOfWork === "New Tool Development (NTD)"`. 9-stage workflow. Per-component approval loops in Stage 7 (DFM) and Stage 8A (Mould Design). Unbounded trial loop in Stage 8C.

See `src/types/ntd.ts` and `src/lib/ntd.ts` for all types and localStorage helpers.
